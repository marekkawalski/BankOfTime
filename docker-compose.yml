version: "3.4"

services:
  bankoftime-frontend:
    image: frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - 3006:80
  bankoftime-backend:
    image: backend
    build:
      context: ./backend/BankOfTimeKotlin
      dockerfile: Dockerfile
    ports:
      - 8080:8080
